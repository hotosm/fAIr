name: Release Chart

on:
  # Push includes PR merge
  push:
    branches:
      - develop
    paths:
      # Workflow is triggered only if chart dir changes
      - chart/**
  # Allow manual trigger
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  publish:
    uses: hotosm/gh-workflows/.github/workflows/just.yml@4.0.3
    with:
      environment: "test"
      command: "chart publish"
    secrets: inherit
