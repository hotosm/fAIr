x-common-env: &common-env
  env_file:
    - ${ENV_FILE:-.env.production}

x-common-volumes: &common-volumes
  volumes:
    - ${LOG_PATH:-/opt/fair-app/data/log}:${LOG_PATH:-/opt/fair-app/data/log}
    - ${RAMP_HOME:-/opt/fair-app/data/ramp}:${RAMP_HOME:-/opt/fair-app/data/ramp}
    - ${TRAINING_WORKSPACE:-/opt/fair-app/data/trainings}:${TRAINING_WORKSPACE:-/opt/fair-app/data/trainings}
    - ${BASEMODELS_PATH:-/opt/fair-app/data/basemodels}:${BASEMODELS_PATH:-/opt/fair-app/data/basemodels}

services:
  postgres:
    <<: *common-env
    image: postgis/postgis:17-3.5
    platform: linux/amd64
    container_name: pgsql
    restart: always
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-ai}
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      - ${POSTGRES_DATA:-/opt/fair-app/data/postgres}:/var/lib/postgresql/data
    ports:
      - "127.0.0.1:5434:5432"
    networks:
      - backend-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    <<: *common-env
    image: redis:8-alpine
    platform: linux/amd64
    container_name: redis
    restart: always
    command: redis-server --appendonly yes
    volumes:
      - ${REDIS_DATA:-/opt/fair-app/data/redis}:/data
    ports:
      - "127.0.0.1:6378:6379"
    networks:
      - backend-network
    healthcheck:
      test: ["CMD", "redis-cli", "-a", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  backend-api:
    <<: [*common-env, *common-volumes]
    image: ghcr.io/hotosm/fair-api:${TAG:-latest}
    platform: linux/amd64
    container_name: api
    restart: always
    command: gunicorn --bind 0.0.0.0:8000 --workers 3 fairproject.wsgi:application
    ports:
      - "8000:8000"
    depends_on:
      redis:
        condition: service_healthy
      postgres:
        condition: service_healthy
    networks:
      - backend-network
    healthcheck:
      test:
        [
          "CMD-SHELL",
          'python -c "import sys, requests; r = requests.get(''http://127.0.0.1:8000/api/''); print(r); sys.exit(0 if r.status_code==200 else 1)"',
        ]
      interval: 30s
      timeout: 10s
      retries: 2

  backend-api-django-q:
    <<: [*common-env, *common-volumes]
    image: ghcr.io/hotosm/fair-api:${TAG:-latest}
    platform: linux/amd64
    container_name: api-django-q
    restart: always
    command: python manage.py qcluster
    depends_on:
      redis:
        condition: service_healthy
      postgres:
        condition: service_healthy
      backend-api:
        condition: service_healthy
    networks:
      - backend-network
    deploy:
      resources:
        limits:
          cpus: "2"
          memory: 2G

  backend-worker-cpu:
    <<: [*common-env, *common-volumes]
    profiles: ["cpu"]
    image: ghcr.io/hotosm/fair-worker:${TAG:-latest}-cpu
    platform: linux/amd64
    container_name: worker-cpu
    restart: always
    command: celery -A fairproject worker --loglevel=INFO --pool=threads --concurrency=1 -Q ramp_training,yolo_training
    depends_on:
      backend-api:
        condition: service_healthy
      redis:
        condition: service_healthy
      postgres:
        condition: service_healthy
    networks:
      - backend-network
    deploy:
      resources:
        limits:
          cpus: "2"
          memory: 4G

  backend-worker-gpu:
    <<: [*common-env, *common-volumes]
    profiles: ["gpu"]
    image: ghcr.io/hotosm/fair-worker:${TAG:-latest}-gpu
    platform: linux/amd64
    container_name: worker-gpu
    restart: always
    command: celery -A fairproject worker --loglevel=INFO --pool=threads --concurrency=1 -Q ramp_training,yolo_training --max-tasks-per-child 1000  -n worker-gpu@%h
    depends_on:
      backend-api:
        condition: service_healthy
      redis:
        condition: service_healthy
      postgres:
        condition: service_healthy
    networks:
      - backend-network
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
        limits:
          memory: 8G

  prediction-worker:
    <<: [*common-env, *common-volumes]
    image: ghcr.io/hotosm/fair-offline-predictor:${TAG:-latest}
    platform: linux/amd64
    container_name: prediction-worker
    restart: always
    command: celery -A fairproject worker --loglevel=INFO --concurrency=1 -Q predictions --max-tasks-per-child 1000  -n prediction-worker@%h
    depends_on:
      backend-api:
        condition: service_healthy
      redis:
        condition: service_healthy
      postgres:
        condition: service_healthy
    networks:
      - backend-network
    deploy:
      resources:
        limits:
          cpus: "2"
          memory: 4G

  flower:
    <<: *common-env
    image: mher/flower:2.0.1
    platform: linux/amd64
    container_name: flower
    restart: always
    command: celery --broker=redis://${REDIS_HOST:-redis}:${REDIS_PORT:-6379}/0 flower --address=0.0.0.0 --port=5000 --basic_auth=${FLOWER_USER:-admin}:${FLOWER_PASSWORD:-admin}
    ports:
      - "5000:5000"
    networks:
      - backend-network
    depends_on:
      backend-api:
        condition: service_healthy
    profiles:
      - cpu
      - gpu

networks:
  backend-network:
    driver: bridge
